name: Build Docker when Push

on:
  push:
    branches:
      - "main"

env:
  IMAGE_NAME: "{{PROJECT_NAME}}"

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set commit SHA
        run: echo "COMMIT_SHA=$(echo ${{ github.sha }} | cut -c 1-7)" >> ${GITHUB_ENV}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Set owner name
        run: |
          GITOWNER=$(echo ${{ github.repository_owner }} | tr '[:upper:]' '[:lower:]')
          echo "GITOWNER=$GITOWNER" >> ${GITHUB_ENV}

      - name: Build and export
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: false
          tags: |
            ghcr.io/${{ env.GITOWNER }}/${{ env.IMAGE_NAME }}:latest
            ghcr.io/${{ env.GITOWNER }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          outputs: type=oci,dest=/tmp/myimage-${{ env.COMMIT_SHA }}.tar

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.IMAGE_NAME }}-${{ env.COMMIT_SHA }}
          path: /tmp/myimage-${{ env.COMMIT_SHA }}.tar
